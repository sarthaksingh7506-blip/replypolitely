<svg xmlns="http://www.w3.org/2000/svg" width="500" height="500" viewBox="0 0 500 500">

  <!-- Background transparent -->
  <rect width="500" height="500" fill="none"/>

  <!-- Bubble -->
  <path d="M150 120a110 110 0 1 1 -30 210l-26 22 10 -42a110 110 0 0 1 46 -190z"
        fill="#2874f0"/>

  <!-- Check -->
  <path d="M120 255l55 55 90 -90"
        stroke="#ffffff"
        stroke-width="25"
        fill="none"
        stroke-linecap="round"
        stroke-linejoin="round"/>

  <!-- Reply (path) -->
  <text x="260" y="230" font-family="Arial" font-size="80" font-weight="700" fill="#2874f0">
    Reply
  </text>

  <!-- Politely (path) -->
  <text x="235" y="330" font-family="Arial" font-size="80" font-weight="700" fill="#000000">
    Politely
  </text>

</svg>
